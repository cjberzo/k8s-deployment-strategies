apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: NodePort
  selector:
    app: myapp   # SIN version
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007